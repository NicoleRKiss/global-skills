<!DOCTYPE html>
<html>

    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Registro</title>

        <link rel = "stylesheet" href = "../stylesheets/styles.css">
        
    </head>
<body class= "body1">

        <form action="/register" method="POST" enctype="multipart/form-data">
        <div class="register">
            <h1>Crear Cuenta</h1>
                <label for="name" class="form-label"></label>
                <input type="text" id="nombre" name="Nombre" placeholder="Nombre" class="form-input">
                <p class="errors">
                    <% if (typeof errors != 'undefined' ) { %>
                            <% if (errors.Nombre) { %>
                            <%= errors.Nombre.msg %>
                        <% } %>
                    <% } %>
                </p>|
            
                <label for="apellido" class="form-label"></label>
                <input type="text" id="apellido" name="Apellido" placeholder="Apellido" class="form-input">
                <p class="errors">
                    <% if (typeof errors != 'undefined' ) { %>
                            <% if (errors.Apellido) { %>
                            <%= errors.Apellido.msg %>
                        <% } %>
                    <% } %>
                </p>
            
                <label for="Email" class="form-label"></label>
                <input type="text" id="Email" name="Email" placeholder="Ej: @gmail.com" class="form-input" value="Email">
                <p class="errors">
                    <% if (typeof errors != 'undefined' ) { %>
                            <% if (errors.Email) { %>
                            <%= errors.Email.msg %>
                        <% } %>
                    <% } %>
                </p>
            
                <label for="password" class="form-label"></label>
                <input type="password" id="password" name="password" placeholder="Password" class="form-input" value="" pattern="(?=.*[A-Z]).{8,}">
                <p class="errors">
                    <% if (typeof errors != 'undefined' ) { %>
                        <%=  errors.password ? errors.password.msg : '' %>
                    <% } %>
                </p>
            
                <label for="password" class="form-label"></label>
                <input type="password" id="password" name="retypePassword" placeholder="Repetir contraseña" class="form-input" Value="">
            
                <p class="minimo">Mínimo 8 caracteres, una letra y un número.</p>
                
                <p><a class="usando">-------------------- O USANDO --------------------</a></p>

            
                <label for="name" class="form-label"></label>
                <input type="text" id="nombre" name="" class="registrarcon" value= "REGISTRATE USANDO GMAIL">
            
                <label for="apellido" class="form-label"></label>
                <input type="text" id="apellido" name="" class="registrarcon" value= "REGISTRATE USANDO FACEBOOK">
                <div class="multer">
                    <label for="image" class="form-label col-12">Avatar:</label>
                    <input type="file" id="image" name="image" class="form-file col-12">
                    <div class="col-12">
                        <p class="error"><%= typeof errors == 'undefined' ? null : errors.image ? errors.image.msg : null %></p>
                    </div>


            <input type="submit" id="Crear cuenta" name="" placeholder="" class="form-Cuenta" value= "CREAR CUENTA">
            <article class="Recordarme">
            <label>
                <input type="checkbox" checked="checked" name="remember"> Recordarme</label>
            </article>

            <p class="antePieRegistro">Al crear esta cuenta usted acepta nuestras Políticas de Privacidad y Términos del servicio</p> 
            </article>
            <br>
            <article class="pieRegistro">
            <p class="pieRegistro"><a class="pieRegistro" href ='login'>Si YA TENES CUENTA HACE CLICK ACÁ</a></p>
             </article>
            </div>
        </form>
    
</body>
</html>
